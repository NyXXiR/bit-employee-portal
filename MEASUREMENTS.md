# Background Check API 실측

> 측정일: 2026-09-03 KST. GET 100회 및 동시성 측정은 현재 진행 중이며, 완료 후 이 문서의
> 빈 값을 원본 JSON에서 채운다. 측정 대기 시간은 과제 작업 시간에서 제외했다.

## 측정 환경

- 호출 위치: 개발 PC, 대한민국
- 대상: Swagger에 기재된 ap-northeast-2 운영 URL
- 런타임: Node.js 20
- HTTP 클라이언트: Node 내장 `fetch`
- 개별 요청 관측 상한: 30초
- 원본 데이터: `measurements/` 아래 `raw.json`, `summary.json`
- 실행 도구: `scripts/measure-background-check.ts`

## GET `/background-checks/{checkId}` 응답 지연

진행 중이다. 완료 후 아래 표에 표본 수와 함께 기록한다.

| 표본 수 | p50 | p95 | p99 | 최대값 |
|---:|---:|---:|---:|---:|
| 100 | 측정 중 | 측정 중 | 측정 중 | 측정 중 |

## HTTP 상태코드 분포

GET 100회 측정 완료 후 코드별 건수와 비율을 기록한다. 클라이언트 타임아웃과 네트워크
실패는 HTTP 응답이 아니므로 별도 항목으로 구분한다.

## 같은 employeeId로 POST를 반복한 결과

`MEASURE-EMP-001`과 동일한 이름·생년월일로 POST 5회를 실행했다.

- 표본 수: 5
- 서로 다른 `checkId`: 5개
- 직원 단위 중복 제거: 관찰되지 않음
- 최종 결과: `flagged` 5건
- 결론: 동일 `employeeId`의 재전송은 새 검사를 만들 수 있으므로 애플리케이션이 자체적으로
  멱등 키와 직원별 활성 검사 유일성을 보장해야 한다.

원본 이력은 `measurements/2026-09-03-recovered/duplicate-post-history.json`에 저장했다.

## pending에서 최종 상태까지 걸린 시간

POST 반복 표본 5건의 `createdAt`과 `completedAt` 차이다.

| 표본 | 최종 상태 | 완료 시간 |
|---:|---|---:|
| 1 | flagged | 0초(즉시 완료) |
| 2 | flagged | 55.322초 |
| 3 | flagged | 0초(즉시 완료) |
| 4 | flagged | 0초(즉시 완료) |
| 5 | flagged | 124.663초 |

- 표본 수: 5
- p50: 0초
- p95: 124.663초
- 최댓값: 124.663초
- 즉시 완료와 2분 이상 소요가 함께 나타나 완료 시간이 크게 분산됐다.

별도의 애플리케이션 연동 스모크 테스트 1건은 `pending`으로 접수된 뒤 `clear`가 됐으며,
외부 응답 시각 기준 완료까지 6.652초였다. 이 POST에서는 Swagger 예시에 있는
`estimatedCompletionSeconds`가 관찰되지 않았다.

## 동시 요청 변화

동시성 1, 5, 10에서 각 20회씩 측정 중이다. 완료 후 각 단계의 p50, p95, p99, 최대값,
상태코드 및 타임아웃 건수를 비교한다.

## Swagger와 실제 동작 대조

현재까지 확인한 내용:

- 명세와 일치: POST는 `checkId`와 `pending` 또는 최종 상태를 반환한다.
- 명세와 일치: GET으로 최종 `clear`/`flagged` 결과를 조회할 수 있다.
- 명세에 없는 중요한 동작: 동일 `employeeId` POST 5회가 서로 다른 검사 5건을 생성했다.
- 명세 설명과 차이: `estimatedCompletionSeconds`를 폴링 결정에 사용하라고 되어 있지만 실제
  스모크 POST 응답에서는 해당 값이 없었다.
- 예시와 실제 상황 차이: 완료 시간이 즉시부터 124.663초까지 분산되어 단일 고정 대기값으로
  설명하기 어렵다.

## 애플리케이션 설정 결정

최종 GET 및 동시성 수치가 나온 후 확정한다.

| 항목 | 값 | 근거 |
|---|---|---|
| 타임아웃 | 측정 중 | GET p99와 타임아웃 표본 확인 후 결정 |
| 재시도 횟수 | 측정 중 | 오류 코드별 회복 여부 확인 후 결정 |
| 재시도 간격 | 측정 중 | `Retry-After` 실제 관측 여부와 지연 분포 확인 후 결정 |
| 폴링 주기 | 측정 중 | 즉시 완료와 장기 pending 분포를 함께 반영 |
| 중단 조건 | 일부 확정 | `clear`, `flagged`, 4xx는 중단. 5xx·503 세부 정책은 측정 후 확정 |

POST는 응답 유실 시 접수 여부를 알 수 없고 외부 API가 멱등 키를 지원하지 않는다. 따라서
POST를 자동 재시도하지 않는다. 애플리케이션에는 `UNKNOWN` 상태로 기록하고 같은 직원의 새
요청을 차단해 운영자가 확인하게 한다.
