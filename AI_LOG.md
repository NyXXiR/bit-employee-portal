# AI 협업 기록

> 이 파일은 작업 종료 시 제출용으로 정리한다. 현재는 대화에서 나온 방향과 변경점을
> 누락하지 않기 위한 작업 기록이다. 비밀번호, 토큰, 로컬 사용자명 등은 제출 전에
> 제거하거나 마스킹한다.

## 현재까지의 협업 요약

### 요구사항 분석

- 과제의 필수 기능, 제출물, API 명세의 빈틈과 6시간 범위를 분리했다.
- 동명이인은 이름이 아니라 사번으로 식별해야 함을 확인했다.
- 생년월일 미확인 직원과 한글 이름 분리가 외부 API 연동의 주요 예외임을 확인했다.
- API의 실제 지연, 중복 요청, 상태 변화, 오류율은 추측하지 않고 측정해야 함을 확인했다.

### 작업 계획

- 설계 결정, API 측정 시작, 조기 배포, 인증·권한, 업무 기능, 외부 API, 검증, 문서화
  순서로 진행하기로 했다.
- AI 거절 사례와 AI 오류 수정 사례는 억지로 만들지 않고 실제 개발 과정에서 발생한
  사례만 나중에 정리하기로 했다.

### 사용자 피드백으로 구체화된 방향

- 퇴사 처리 후 기존 로그인 세션도 다음 요청부터 차단한다.
- 한글 이름을 자동 추론하지 않고 성과 이름을 강제 입력하며 예외는 관리자가 처리한다.
- 생년월일 누락이 사이트 장애로 이어지지 않게 하고, 직원에게 빠른 보완을 유도하며,
  관리자가 누락을 발견하고 대신 입력할 수 있게 한다.
- Background Check의 반복 및 동시 요청은 DB 충돌 방지와 백엔드 도메인 계약을 모두
  사용해 통제한다.

구체적인 결정과 구현 계약은 `docs/PROJECT_DIRECTION.md`와
`docs/DECISION_LOG.md`를 참조한다.

## A. AI 제안을 거절하거나 되돌린 지점

### 작업 메모: 별도 Next/Nest 모노레포 제안 철회

- AI의 제안: Next.js 프론트엔드, NestJS 백엔드, 공통 계약 패키지로 구성한 모노레포
- 내가 선택한 것: 단일 Next.js 프로젝트에서 UI와 서버 계층만 코드로 분리
- 판단 근거: 별도 실행 단위가 인증, 배포, CORS와 타입 동기화 비용을 늘리며 6시간 과제의
  핵심인 권한과 외부 API 안정성 검증에 직접 기여하지 않음
- 결과: 설치 전 모노레포 골격을 철회하고 단일 Next.js 구조로 변경함

나머지 사례는 개발 과정에서 실제 사례가 발생하면 기록한다.

## B. AI가 처음 잘못 만들어서 고친 지점

구현 후 테스트, 로그, API 실측 또는 코드 리뷰로 발견한 실제 사례를 기록한다.

## C. 가장 설명하기 어려운 코드

구현 완료 후 본인이 실제로 설명하기 어려운 부분을 솔직하게 기록한다.
